language: php

php:
    - 7.3

services:
    - mysql


install:
    - mysql -e 'create database zhiyuan;'
    - composer self-update
    - composer install --no-interaction --prefer-dist --no-suggest
    - php artisan key:generate
    - php artisan migrate
    - php artisan passport:install

